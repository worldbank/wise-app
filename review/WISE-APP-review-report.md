# WISE-APP Comprehensive Review Report

**Scope:** Whole repository (`R/`, `batch/`, `dev/`, `tests/`, `inst/`, docs), plus live Chrome desktop verification
**Date:** 2026-08-28 · **Reviewed Repo State:** `main` @ `cf1cb48` · **Current Remediation Base:** `golem`
**Target Context:** Wider release beyond core team; primary performance bottleneck is Step 2 simulation.
**Deployment Model:** Posit Connect (git-backed, 1 session/process, Databricks backend, auto-connect — see §10.1) & Local R package (single user, custom data connection). Synchronous execution, desktop browser.

> **Remediation status (through 2026-09-02):** Waves 0 and 0.5 are complete; most Wave 1-4 items are fixed. Completed rows are compressed under their sections, open rows keep full detail. On 2026-09-01, 17 batches landed: deployment/test blockers (DEP-01..03, INT-02/09, REACT-05, SEC-02, TEST-06..09) · output-neutral perf batches A-D (PERF-08/10/13/22/23/23b/25/30/32/34, DUP-01 — all equivalence- or bit-identical-verified) · quick wins (INT-04/07, UI-04/38, RED-05) · three High-severity fixes (REACT-02 busy guards, REACT-12 failure ledger, UI-01 visible errors, PERF-31 aggregation cache) · standalone perf wins (PERF-13/16/23) · the Wave 0.5 completion batch (INT-01/05/06/08) · a Step 1 responsiveness batch (button-snapshot rendering in Outcome/Weather stats + survey-reload staleness banners) · a threshold-table matrix fix (transposed per-model RP matrix with a single admissible RP). On 2026-09-02, the Wave 5 grouped-aggregation items PERF-05/09/33 landed as collapse-based rewrites (see §5.2). Also on 2026-09-02: a dedup batch (REACT-08 shared coefficient/term helpers, REACT-09 run-trigger parameter, RED-08 dev/ scratch scripts archived) and a High-severity honesty batch (REACT-14 structured fallback metadata + provenance banner + modal acknowledgement for model-family fallbacks; UI-29 Steps 1/2 run buttons disabled with prerequisite explanations); then a flyout/a11y batch (UI-02 shared flyout block with one-open/focus/Escape/anchored positioning, UI-28 closed per user decision, UI-03 accessible names for all `label = NULL` controls, UI-36 alt text on all 21 plots + labelled map regions), and a Step 2/3 results-controls alignment: the Step 3 results pane now uses the same aggregation/filters section as Step 2 — identical conditionalPanel poverty-line cell (removed from the flex row, not left as an empty slot, for non-poverty methods; the run's poverty line seeds the value until the user edits it), SSP x period checkbox grid with UI-38 held selection, matching panel padding and Advanced-section structure — while keeping its INT-01 selection-restore behavior. Also on 2026-09-02: PERF-26 (month/year extraction from simulation timestamps now uses one truncating POSIXlt pass in a shared `.add_sim_timestamp_fields()` helper; no fractional-second boundary rolls) and UI-41's Step 3 chart/table outputs restored to `suspendWhenHidden = TRUE`; then a map/interaction lag batch — PERF-36 (DuckDB-computed bounds beside geometry, single per-feature-style GeoJSON layers on the outcome/continuous-weather maps, canvas on the survey-locations map; also fixed the outcome coverage map silently drawing no polygons) and REACT-03 (digest short-circuit of identical Step 1 load clicks), plus a follow-up fix keying `map_view_memory()` to the data identity so a sample-country switch re-fits the map instead of restoring the previous country's viewport; then a basemap migration moving all four maps from Leaflet + watermarked raster Carto tiles to MapLibre GL (`mapgl`) with the keyless CARTO vector Positron style, and a vectorized FeatureCollection serializer for the MapLibre sources (13-17x widget-build win; see §5.1). Remaining: the open rows below, Wave 5 golden-file refactors (PERF-02/15), and environment-only verifications (DEP-01/02 live backends, PDF manual). Suite: **716/716 pass**, no warnings; `R CMD build` clean. Deployment commits `0b73a4c`/`459d3f7`/`fdd3960` — see §10.1. **MapLibre rollback (2026-09-02, later the same day):** the Connect build could not install `mapgl` (hard sf/terra/geojsonsf Imports) and the host's GDAL stack is broken at runtime (`libgdal.so.36` missing → `r-missing-system-library`), so all four maps were rolled back to **leaflet** while keeping the migration's wins: per-feature colours via leaflet's native `feature.properties.style` merge in `addGeoJSON` (one layer per map), the vectorized FeatureCollection serializer, DuckDB-computed bounds, view-memory keying, in-place wave swaps (`leafletProxy` + `removeGeoJSON`/`addGeoJSON` with a per-wave FC-string cache), the wave toggle slider, and the reset control. Basemap is raster CARTO Positron again (keyless; `Esri.WorldGrayCanvas` is the documented fallback if tiles retire). `mapgl` removed from DESCRIPTION/renv.lock; `geojsonsf`/`classInt` drop from the manifest, `sf` re-stripped, `terra` retained (raster dependency, builds without GDAL) — see §10.1.

---

## 1. Executive Summary

The codebase has a sound modular architecture (Golem structure, pluggable engine registry in `fct_fit_model.R`, Bootstrap 5 / bslib UI). Three gaps still block wide release:

1. **Result Integrity & State Synchronization:** Upstream changes trigger `renderUI` rebuilds that wipe selections, and completed downstream results remain presented after their inputs change. ✅ Closed 2026-09-01: dynamic inputs restore selections (INT-01), results bind to fit-time snapshots (INT-05), map state clears on reload/failure (INT-06), and run signatures with stale-marking now link Step 1 fits, Step 2 simulations, and Step 3 policy results (INT-08). (Reproducibility ✅ Wave 0 complete.)
2. **Release Readiness & UX:** Export is limited to two threshold-table CSV buttons (UI-23), configuration cannot be saved/restored (UI-24), and collapsing the Lasso forced-covariates panel can silently change the apparent model contract (UI-10). Run-button no-ops (UI-29 ✅) and flyout/label a11y gaps (UI-02/03/36/28 ✅) were closed on 2026-09-02.
3. **Packaging:** `R CMD check` is clean at the code level (TEST-08 ✅, TEST-07 ✅); remaining check findings are environment-only (no `pdflatex` for the PDF manual on the check machine).

---

## 2. Priority Implementation Roadmap

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│ Wave 0: Reproducibility & Determinism (DET-01..09) [COMPLETE 2026-08-31]         │
└────────────────────────┬─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────────────┐
│ Wave 0.5: Integrity/deployment blockers [COMPLETE 2026-09-01]                    │
└────────────────────────┬─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────────────┐
│ Wave 1: Reactivity & Correctness Quick Wins (REACT-01..14, DUP-01..02, PERF-07)  │
└────────────────────────┬─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────────────┐
│ Wave 2: Safe speedups (PERF-03/06/08/10/11/13/17/19..32/34/36/37)                │
└────────────────────────┬─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────────────┐
│ Wave 3: Release readiness - UI-01..04/10/23..26/28..36/38..41 + PERF-16          │
└────────────────────────┬─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────────────┐
│ Wave 4: Packaging, tests & repository hygiene (TEST-01..09, RED-01..09)          │
└────────────────────────┬─────────────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────────────┐
│ Wave 5: Validated Numerical & Structural Refactors [PERF-05/09/33 ✅ 2026-09-02] │
│ (remaining open: PERF-02/15)                                                     │
└──────────────────────────────────────────────────────────────────────────────────┘
```

---

## 3. Reproducibility & Determinism (`DET-*`) — ✅ COMPLETE (2026-08-31)

All fixed with isolated `withr::with_seed` streams, deterministic ordering, and regression coverage in `tests/testthat/test-determinism.R` (repeatability, seed sensitivity, exact caller RNG restoration); weather end-to-end tests in `test-fct_get_weather.R`. DET-01 policy seeding · DET-02 residual draws + run-time mode snapshot · DET-03/04 no global `set.seed`/RNG-consuming names · DET-05 dormant engines seeded · DET-06 app/batch Lasso aligned · DET-07 DuckDB single-thread invariant · DET-08 `collect_deterministic()` canonical ordering · DET-09 committed determinism tests replace the OneDrive-only script.

---

## 4. Result Integrity & State Synchronization (`INT-*`) — ✅ COMPLETE (2026-09-01)

**Done, compressed:** INT-01 — `.restore_selection()`/`.restore_numeric()` helpers; every dynamic input (Step 1 year selectors, weather selector, model panel incl. Lasso force/advanced, Step 2 baseline survey, Step 3 poverty line + scenario filters) restores its prior selection clipped to the new choices, falling back to the old defaults only when nothing survives. · INT-08 — immutable run signatures on fit/sim/policy results (survey version + config digests via `.sig_plain()`), staleness observers with a Step-2→3 cascade, `.stale_banner()` on all result surfaces, CSV exports dropped while stale (stale-mark semantics per user decision). · INT-05 — fit results carry a fit-time `.snap` (outcome/weather/variable labels + survey-weather frame); all Step 1 renderers bind to it; engine-conditional headings are reactive; Step 3's historical label is bound to the run. · INT-06 — map/cell state cleared on survey reload and every failure; the `loc_id_panel` warning states the inference implication. · INT-02 — connect failure ledger; the connection publishes only when every file loads. · INT-03 — aggregators prefer the run-time residual snapshot. · INT-04 — Step 3 aggregation failures surface as a persistent toast naming dropped scenarios. · INT-07 — Results/Diagnostics tabs track the `hist_sim` lifecycle. · INT-09 — Step 3 publishes atomically after simulation + decomposition both succeed.

---

## 5. Backend Performance & Safe Speedups (`PERF-*`)

### 5.1 Open Bottlenecks (Wave 2 — output-neutral unless noted)

Open: none. All Wave 2 bottlenecks below are done.

**Done (2026-09-02), compressed:** small verified wins — PERF-03 (KDE hoisted out of the 9-tau loop) · PERF-06 (dead `.feature_centroid()` deleted) · PERF-07 (duplicate diagnostics append) · PERF-17 (`rbind` → `bind_rows`) · PERF-19 (dormant-path precompute) · PERF-20 (shallow-copy fix) · PERF-21/24 (hoisted format/filter loops) · PERF-27 (training `ecdf()` cached per key) · PERF-28 (`requireNamespace()`) · PERF-29 (dead `bins_df` build) · PERF-37 (stale comments).

**Done (2026-09-01), compressed (Batches A-D + standalone; all output-neutral, equivalence/bit-identical verified):** PERF-08 (single grouped `rowsum()` completeness pass, ~3x at 200k×72) · PERF-10 (parsed GeoJSON copies dropped from feature builders) · PERF-13 (bounded versioned disk cache for every remote weather parquet: digest-keyed local slices, one fetch per SSP file shared across baseline overlap + all periods, 2 GB LRU cap, `WISEAPP_WEATHER_CACHE_DISABLE`/`_FORCE` switches; bit-identical cold+warm) · PERF-22 (policy deltas + training ecdf precomputed once per run; 1.5x at 50k HH, bit-identical) · PERF-23 (remote `h3_df` view materialised into a local DuckDB temp table for the six downstream scans) · PERF-23b (hash-cached secrets for S3/GCS/Azure) · PERF-25 (grouping hoisted out of the per-variable weather loop) · PERF-30/REACT-07 (display-only `band_q` dropped from the aggregation workspace; poverty-line/bandwidth folded into method-specific cache keys) · PERF-31 (Step 3 aggregation cache keyed by source/method/poverty line, deviation applied downstream; cached ≡ uncached bit-identical) · PERF-32 (prediction frame released before the N×K build; byte-identical) · PERF-34 (ID→residual lookup + variance built once per pipeline; bit-identical) · PERF-16 (dev mode removed entirely per user decision). Regression coverage: `test-mod_2_02_results.R`, `test-policy-sim-compare-agg-cache.R`, `test-fct_wx_cache.R`, `test-fct_get_weather.R`.

**Done (2026-09-02), compressed:** PERF-36 — the map pipeline now computes bounds beside geometry in DuckDB: the H3 aggregation queries emit per-location and per-cell envelope columns (`ST_Extent` + `ST_XMin/YMin/XMax/YMax` over the same union/geometry) that ride along in feature `properties$bbox`, so `.geojson_bounds()` serves every H3 map from a numeric min/max instead of `fromJSON`-parsing each geometry string in R (measured 18x at 2,000 features: 230 ms → 13 ms). The outcome-coverage map and the continuous weather-location map emit **one** `addGeoJSON` layer carrying per-feature styles instead of one layer per distinct shade (measured 351 → 1 layers at 2,000 locations on a continuous ramp; binned weather was already few-layer), and the survey-locations map now enables the canvas renderer like the other maps. The rewrite also fixed a latent defect: the outcome map passed geometry-less feature lists straight to `addGeoJSON`, which `L.geoJSON` silently skipped, so the map drew basemap tiles but no polygons. Regression coverage in `test-geojson-bounds.R`; the `deckglgeoarrow` option (§5.3) stays gated behind the agreed benchmark targets. Follow-up (same day, user-reported): `map_view_memory()` now takes a data key (digest of the selected sample / outcome spec / weather spec) — the recorded view is reapplied only while the key is unchanged, so switching sample country re-fits the map to the new geography instead of restoring the old viewport, while same-data re-renders (view toggle, wave switch, same-sample reload) still keep the user's pan/zoom. Coverage in `test-map-view-memory.R`. Serialization follow-up (same day): the four map builders assemble their FeatureCollection strings with vectorized column-wise property serialization (`.json_vec()`/`.bbox_frag()`/`.geojson_fc_string()` in `fct_surveystats.R`), replacing the per-feature `sprintf` + `jsonlite::toJSON` pair that dominated widget build time — measured 329 → 25 ms at 2k features (13.2x) and 1588 → 91 ms at 10k (17.5x), bringing the 10k server build under the §5.3 ≤300 ms target without any backend change. Property numbers now keep full precision instead of toJSON's 4-decimal rounding (bbox bounds gain accuracy). Basemap migration (same day, user-reported Carto "API key required" watermarks): all four map builders moved from Leaflet + raster Carto Positron tiles to **MapLibre GL via `mapgl`** with the keyless CARTO **vector** Positron style (`carto_style("positron")` — vector is unaffected by the watermark; raster tiles are being retired, per <https://docs.carto.com/faqs/carto-basemaps>). Per-feature colours travel as `__fill`/`__stroke` properties read through `get()` expressions in a single fill/line layer; the survey map's hover highlight uses MapLibre feature-state (`hover_options`); popups/legends/reset moved to mapgl equivalents (`popup = "{popup}"`, `add_control(html =)` with the existing compact-legend HTML, `add_reset_control`); `map_view_memory()` needed no change (maplibre reports the same `_center`/`_zoom` input shape as Leaflet, and restore() now overrides the widget's initial center/zoom in place of Leaflet's setView). The Leaflet-only `.add_reset_button`/`.map_autofit_js` helpers were deleted (MapLibre tracks container resizes natively). Visual-QA fixes (same day, user review): (1) popups/tooltips were removed from all four maps entirely per user decision (unnecessary visual clutter; the legend and colour encoding convey the message) and per-feature popup properties were dropped, shrinking every map payload; (2) the Step 1 map no longer rebuilds its widget on wave changes — the wave picker swaps the GeoJSON source in place via `maplibre_proxy` + `set_source()` (resolving the density-map wave switch lag); (3) legend control position strings were fixed to MapLibre-hyphenated `"bottom-right"` / `"bottom-left"` (unhyphenated Leaflet position strings were silently rejected by MapLibre's control container, preventing legends from attaching to the DOM). Visual-QA fixes (same day, user screenshot): (1) `{brace}` popup templates HTML-escape interpolated values, so the density map's HTML popups rendered as literal markup — popups/tooltips now use the `get("popup")` **expression** form, which `setHTML`s unescaped; (2) custom HTML controls default to the `maplibregl-ctrl-group` chrome, which collapsed the compact legend into a ~29px circle — controls now pass a bare `className` (and explicit ids, since the weather map's two controls both defaulted to `"custom"` and overwrote each other); (3) navigation zoom controls restored for Leaflet parity, same-colour hairline `fill_outline_color` removes the anti-aliasing seams between edge-to-edge cells, and hover tooltips now cover every layer. Follow-up (same day, user review): popups/tooltips were removed from all four maps entirely — the information is not meaningful in context, the legend and colour encoding already carry it, and dropping the per-feature popup properties shrinks every map payload; and the Step 1 map no longer rebuilds its widget on wave changes — the wave picker swaps the GeoJSON source in place via a `maplibre_proxy` + `set_source()` update (no basemap/style re-fetch, no view reset), which was the "sample density map takes a long time to load" complaint.

### 5.2 Numerical Refactoring (Wave 5 — Requires Golden-File Validation)

| ID | Location | Potential Risk | Actionable Fix |
|---|---|---|---|
| **PERF-02** | `R/fct_get_weather.R:138-175, 532-533, 829-831` | Restructures SQL query plan; altered float sum order | Collapse V separate scans/joins into single wide `summarise(across(...))` + single join. |
| **PERF-15** | `R/fct_simulations.R:664-665` vs `R/fct_predict_outcomes.R:154-157` | Modifies `fixest` row-dropping & offset handling | Reuse design matrix between prediction and uncertainty factor loading. |

**Done (2026-09-02), compressed (collapse throughout; old-vs-new parity harness run on adversarial synthetic data):** PERF-05 (`summarise_weather_by_loc()` and `merge_loc_values_to_cells()` rebuilt as grouped `collapse` passes over one shared `GRP()` — `.summarise_loc_prep()` now returns the grouping instead of split indices; continuous means, weighted/unweighted modal bins with their tie-break orders, `n_hh`/`n_months`/`n_locs` semantics and row order all preserved exactly; 69x on the Step 1 weather-map collapse at 50k rows x 10 vars, 5.9x on cell merge) · PERF-09 (wave-specific `% Missing` for all variables in one grouped pass via a new shared `survey_missingness_long()` helper used by both Step 1 stats tables; exact parity; ~25x) · PERF-33 (`weighted_summary_long()` rebuilt as six grouped `collapse` matrix passes — `fmean`/`fsd`/`fmin`/`fmax`/`fnobs` — with the app's `is.finite(x) & is.finite(w) & w > 0` mask folded in, NA-key rows dropped as `split()` did, and all-masked (countryyear, variable) cells still emitted with N = 0; weighted SD now uses `fsd(w=)` with the $\sum w-1$ denominator, accepted per user decision; 6.5x at 50k x 24, 2.2x at the 200k x 72 production scale). `collapse` 2.1.7 declared in `DESCRIPTION` Imports and `renv.lock` (`Rcpp` already locked). Follow-up collapse wins in the same pass: `.compute_hazard_values()` (`R/fct_policy_decompose.R`) and `summarise_weather_anomaly_by_loc()` (anomaly/percentile historical view — bit-exact parity incl. the old `weighted.mean` NA-weight poisoning semantics) now grouped passes (35x at 400k rows x 40k locations), the Step 3 decomposition channel aggregation (`mod_3_09_decomposition.R`) shares one grouping across channels instead of re-splitting per channel, and the binned-weather DT hoists its per-variable missingness into the shared helper. The Step 1 stats-table `Variable` column change is recorded under §7. **PERF-26 (2026-09-02):** `int_month`/`sim_year` are derived from simulation timestamps in one `as.POSIXlt()` pass via a shared `.add_sim_timestamp_fields()` helper used by both the simulation join and `prepare_hist_weather()` (two duplicated `format()` passes deleted); POSIXlt truncates, so fractional-second timestamps can no longer round across a month/year boundary, while whole-second `Date` output is unchanged. Regression coverage in `test-prepare-hist-weather.R` (12-month equivalence with the old `format()` output, boundary truncation, NA-timestamp drop).

### 5.3 Map Rendering Decision Gate

1. Benchmark current Leaflet on representative 1k, 10k, and maximum-production polygon sets: server build time, serialized payload bytes, browser first paint, pan FPS, hover/popup latency, peak browser memory.
2. ~~Apply the remaining output-neutral fixes (PERF-36; PERF-10 already shipped) and repeat the benchmark.~~ ✅ PERF-36 shipped 2026-09-02 (see §5.1); only the benchmark repeat remains.
3. Prototype `deckglgeoarrow` only if optimized Leaflet misses agreed targets: identical classification/legend values, tooltips, map view, Shiny proxy updates, no runtime CDN, fresh-clone Connect deploy, non-WebGL fallback. Keep geometry server-mediated; never expose storage credentials/tokens or direct browser URLs to restricted GeoParquet.
4. Provisional targets at 10k polygons: exactly one data layer, server widget build <=300 ms, time-to-interactive <=1.5 s, sustained pan/zoom >=45 FPS. Pursue WebGL/GeoArrow only if optimized Leaflet fails the maximum-production or 50k-polygon probe.

**Done (2026-09-03), compressed — MapLibre/H3 cell migration (§5.3 remediation):** all four Step-1 maps now render from a **vendored MapLibre GL 5.24.0 + h3-js 4.1.0 engine** (`inst/app/www/vendor/`, sha256 pins recorded in `R/fct_hexmap.R`; no CDN at runtime, no new R packages, `mapgl` stays out). R sends a columnar payload per map — cell ids + values + ramp stops (`hexmap_payload()`/`hexmap_update()`/`hexmap_clear()`/`hexmap_fit()` in the new `R/fct_hexmap.R`) — and `inst/app/www/hexmap.js` decodes each H3 index to a polygon with h3-js and paints one GeoJSON source whose colour is an `interpolate`/`match` expression (log ramps ride a precomputed `v_log` column; NA cells paint the grey `na.color`; a 1px same-colour line layer kills the inter-cell seams; averaged weather cells get a dashed `hex-dash` layer from a `dash` column; an optional `info` column feeds hover tooltip identifiers). The wire contract is columnar ids/values — geometry never leaves the browser as anything but H3 strings, and no storage URLs/credentials reach the client (§5.3 gate honoured). Engine behaviour: lazy boot on first payload (no basemap fetch for maps never shown), boot-once per container, load-order tolerant + idempotent against `bundle_resources()`'s alphabetical re-scan, queued messages for containers that appear after their payload (`renderUI` flush order), camera persistence across `set` with `fit` sent only on data-key change (PERF-36 view-key semantics — wave toggles re-colour in place), WebGL2 capability probe writing `input$<id>_webgl` with the Leaflet builders kept as a runtime fallback (rendered from `cell_data$geom`, INT-06-consistent notices when no cells exist). Wired surfaces: sample density (`mod_1_02`, Leaflet fallback + `map_view_memory` retained), outcome coverage (`mod_1_03`, R-side legend overlay from the shared palette state, per-cell tooltip identifiers from `cell_map`), and the per-variable weather maps (`mod_1_05`, shared across-wave palette via the new `wxmap_pals()` reactive so scales do not shift between waves; fallback map harness updated to the cell-data fixture). **Payload contract fields:** `{id, action: set|clear|fit, h3[], v[], v_kind, stops, v_log?[], info?[], dash?[], bounds, label, unit}`. **Benchmarks** (`dev/archive/bench_maps.R` + output): payload build 3 ms (1k) / 15 ms (10k) / 7 ms (50k) for density and 91 ms (1k) / 559 ms (10k) / 2.6 s (50k) for coverage — production-scale (~1k cells) meets the ≤300 ms target on both; wire bytes ~35 KB per 1k cells (vs full geometry strings before). The 10k/50k coverage cost is dominated by the pre-existing `merge_loc_values_to_cells()` weighted merge (shared with the Leaflet path — not a migration regression); flagged as the hotspot to revisit only if >10k-cell loads materialise. **deck.gl contingency not needed and reverted** (2026-09-03): a vendored deck.gl 9.3.11/9.1.15 `H3HexagonLayer` prototype was built and abandoned — deck.gl renders nothing under this environment's GPU stack (blank even for a one-point ScatterplotLayer, headless and headed), and the plain MapLibre engine met the targets; `deck.gl.js` is not vendored. Manual-QA checklist per phase (render, wave toggle, reload-clear, click, fallback, reset, tooltip) is automated in the hexmap browser harness; suite after migration: **798/798 pass, no warnings** (includes the previously failing `test-mod_1_05_weatherstats.R` colour-parity assertions, whose widget extraction still read the mapgl-era `__fill` property — fixed to the Leaflet `style.fillColor` call format). `filter_features_by_wave()`, `plot_survey_map()` and `.survey_fc_string()` deleted with the location-outline maps; `map_data` removed end-to-end from the module APIs and `st_union_agg` from `globals.R`. ---

---

## 6. Reactivity & Pipeline Correctness (`REACT-*`)

| ID | Issue & Location | Severity | Actionable Fix |
|---|---|---|---|
| **REACT-11** | Parallel aggregation engines in `R/mod_2_02_results.R:287-517` vs `R/fct_policy_sim_compare.R:412-527` | **Low** | Consolidate Step 2 and Step 3 aggregation into unified `fct_aggregation.R` engine. |

**Done, compressed:** REACT-01 (`return()` bug fixed; local-path validation closed by DEP-03) · REACT-02 (`.busy_guard()` refuses re-entry and disables buttons at all six load/run sites; error-path release tested) · REACT-05 (decomposition failures fail the run with a persistent alert; partial per-scenario-year failures publish with a warning naming the dropped count) · REACT-07 (closed via PERF-30) · REACT-10 (2026-08-31) · REACT-12 (per-key failure ledger in `fct_run_simulation()`: throws on historical or all-member group failure so unusable results never publish; partial failures publish with a persistent warning + `n_models`/`n_models_requested` provenance; TEST-01 remainder) · REACT-13 (hero point-range scenario filter fixed; module harness added). **Closed, no action:** REACT-04 (replacement semantics are by design) · REACT-06 (declined per review guidance).

**Done (2026-09-02), compressed:** REACT-08 — the five copies of term extraction (`extract_cov_names` in mod_3_02..05, `.model_term_names` in mod_3_06) collapsed into `model_covariate_names()` / `model_coefficient_reactives()` / `model_term_names()` in `utils_mod_1_helpers.R`; NULL-in/NULL-out contract of `apply_policy_to_svy()` gating preserved (NULL = no gating). · REACT-09 — `mod_3_06_policy_sim_server()` takes a `run_trigger` reactive; the parent passes `req(input$run_policy_sim)` and no longer calls the exported `run()` closure (the `run` export was removed). `req()` on the dynamically rendered button also stops the spurious first-fire run (previously the persistent alert appeared on first Step 3 visit). · REACT-14 — `fit_model()` records structured `fallbacks` metadata (model-family and VCV fallbacks with requested/used/reason); Step 1 results show a persistent provenance banner and a model-family fallback additionally raises a non-dismissable modal requiring explicit "I understand" acknowledgement. Direct coverage in `test-fct_fit_model_fallback.R` + banner test in `test-mod_1_07_results.R`. · REACT-03 (2026-09-02) — both Step 1 load buttons digest their full request (selected surveys, connection params, variable metadata + CPI/PPP; weather adds the survey version standing in for the frame) and short-circuit identical repeat clicks with a "already loaded" notification instead of re-running the DuckDB I/O. The signature is stored only when no inner stage warned, so a partially failed load (H3 build, sample density, `loc_id_panel`) always retries on the next click; INT-06 clear-on-reload semantics are untouched (the short-circuit returns before any state is cleared).

---

## 7. UI, UX & Accessibility Findings (`UI-*`)

| ID | Issue & Location | Severity | Actionable Fix |
|---|---|---|---|
| **UI-02** | Config flyouts (`mod_1_04:87-239`, `mod_1_06:28-48`, `mod_2_01:43-57`) share fixed `left: 400px; top: 90px`, can remain open across accordion switches, lack focus/Escape handling | **High** | ✅ Closed 2026-09-02 — see Done below. |
| **UI-03** | Visual headings/`tags$label` not associated with controls whose Shiny `label = NULL` (`R/mod_2_01_weathersim.R:69-157, 310-323`, `mod_2_02:813-821`, `mod_3_01_sp:117-194, 325-345`, Step 3 helpers incl. `mod_3_04_labor:126-206`) | **High** | ✅ Closed 2026-09-02 — see Done below. |
| **UI-10** | Collapsing the Lasso forced-covariates panel (`R/mod_1_06_model.R:456-474, 511-617`) removes inputs; live `selected_model()` changes the apparent model contract while `model_fit` stays unchanged | **High** | Keep inputs registered via CSS visibility, or snapshot forced-in/out + advanced settings at fit time; all downstream consumers use the snapshot. ✅ Closed 2026-09-05 - wasn't an issue actually |
| **UI-23** | Export incomplete: only Step 2/3 threshold DTs expose CSV (`R/mod_2_02_results.R:1276-1284`, `R/fct_policy_sim_compare.R:1047-1055`); other tables, plots, configuration, results bundle have no export | **High** | Phase 1: CSV for aggregated plot/table data, server-rendered PNG from the same ggplot builder (fixed dims/DPI), config JSON, provenance sidecar. Prefer base PNG or declare `ragg` + lock entry. No widget screenshots; no raw household-row export without a disclosure policy. ✅ Closed 2026-09-05 - all tables export as CSV |
| **UI-24** | Configurations cannot be saved/shared/restored (`enableBookmarking` inactive) | **High** | JSON export/import of complete analysis configuration state (inputs, model specs, random seed). ✅ Closed 2026-09-05 - configuration.json export/import shipped with the UI-23 bundle |
| **UI-25** | Steps 1-3 openable in any order without prerequisite indicators | **Med-High** | Completion badges (✓) in nav header; "Prerequisites: ..." notices in overview empty states. ✅ Closed 2026-09-05 - dynamic completion checks and reload marks added |
| **UI-26** | Summaries/headers omit source identity, exact model specification, seed, run signature (`mod_1_04:292-338`, `mod_1_06:107-147`, `R/mod_2_01_weathersim.R:340-393`, `mod_2_02:759-783`, `R/fct_policy_sim_compare.R:944-966`) | **Medium** | Standardize provenance banners from immutable result metadata. ✅ Closed 2026-09-05 - summary metadata can be exported |
| **UI-32** | Social protection targeting inputs (`mod_3_01_sp.R:197-216`) lack pre-run feedback on eligible population size | **Medium** | Dynamic text output with estimated eligible household count based on active cutoff. ✅ Closed 2026-09-05 - dynamically adjusted indication in sidebar added |
| **UI-41** | All charts are static ggplots; global widget conversion would serialize household-level values and can misrender complex geoms (21 sites; aggregated candidates at `R/mod_2_02_results.R:1246-1344`, `R/fct_policy_sim_compare.R:1017-1115`, `R/mod_3_09_decomposition.R:134-201`) | **Medium, confidentiality/rendering risk** | Keep static ggplot canonical. After UI-23, prototype interactivity only for aggregated charts; compare `ggiraph` vs `plotly` empirically (neither declared/locked). ✅ 2026-09-02: `suspendWhenHidden = TRUE` restored on all four Step 3 chart/table outputs (`fct_policy_sim_compare.R` — the fourth site beyond the three originally listed), matching the Step 2 pane. Household-level plots stay server-rendered with CSV/text equivalents. |

**Done (2026-08-31), compressed:** UI-30 (inverted future-period warning) · UI-31 (~650 dead lines deleted) · UI-33 (access-attainment relabel) · UI-34 (manufacturing+services >100% warning) · UI-35 (obsolete repo URLs; note the remote is now `worldbank/wise-app` after the rename) · UI-39 (nav link `aria-label` + `rel="noopener"`) · UI-40 (coefficient-uncertainty help corrected).

**Done (2026-09-01), compressed:** UI-01 (output errors visible as warning cards; all long renderers audited — `req()`-guarded or NULL-safe on the pre-run path) · UI-04 (Okabe-Ito palettes centralized in `utils_plot_theme.R`; SSP colours bluish green/blue/vermillion) · UI-38 (Step 2 scenario grid can never drop its last selection).

**Done (2026-09-02), compressed:** Step 1 survey/weather stats tables (incl. the binned-weather table) now show a single `Variable` column — the readable variable label, falling back to the raw name when no label exists — instead of a raw name column plus a separate `Variable Label` column. UI-29 completed: the Step 1 `Run model` button (`mod_1_06`) is disabled via `updateActionButton(disabled=)` and a persistent `alert-warning` names the missing prerequisites (outcome, weather selections, loaded data, model type); the Step 2 `Run simulation` button (`mod_2_01` `run_sim_ui`) renders disabled with the same prerequisite banner (outcome, weather, loaded data, baseline survey, historical period, fitted Step 1 model). Step 3's persistent check alert (2026-08-31) is unchanged. UI-02 closed: all four flyout sites (per-variable weather Configure + Historical comparison in `mod_1_04`, Model settings in `mod_1_06`, Simulation settings in `mod_2_01`) now use a shared `config_flyout_block()` helper — toggle button carries `aria-expanded`/`aria-controls`, the panel is marked `data-flyout-for`, and new `custom.js` behavior enforces one-open-at-a-time, moves focus into the flyout on open and back to its toggle on close, closes on Escape, and positions each open flyout beside its own toggle (following scroll) instead of a shared viewport spot, which also removes the stale-open-across-accordion-switch behavior. UI-03 closed: every `label = NULL` control in the named surfaces now has an accessible name — visually-hidden `span.visually-hidden` labels inside Shiny's native label element (baseline survey, historical period, projection-period year numerics, climate scenarios, simulation residuals, program type, targeting, budget mode, health mode, scenario-grid checkboxes) and `for` attributes added to the existing visible `tags$label` headings for the Step 3 labor sliders; visible grouped headings preserved. UI-36 closed: all 21 `plotOutput()` sites render through `wise_plot_output()` (or the `alts` parameter of `weather_plot_layout()`), which adds `role="img"` + a descriptive `aria-label` on the plot's own container (no extra DOM, bslib layout unchanged); per-variable panels build the label from the live variable names, fixed-id plots describe plot type and content. The three Leaflet maps keep their adjacent stats tables / distribution plots as the text equivalents; an attempt to add `aria-label` regions to the map containers was reverted after the labels rendered as visible stray text (`leafletOutput()` returns a tag list, so appended attributes leak as text children). UI-28 closed **per user decision**: the core Step 2 settings (baseline survey, periods) stay inside the Simulation settings flyout — most users keep the defaults, so no relocation.

**Done (2026-09-05), compressed:** **UI-10** — investigated and closed as *not a defect*. Shiny's `unbindInputs()` (shiny.js) deregisters a removed input's binding and unsubscribes it but never calls `setInput(id, null)`; only *outputs* notify the server when hidden (`sendOutputHiddenState`, the mechanism `suspendWhenHidden` keys off). Collapsing the flyout therefore leaves `input$force_in_*` / `input$force_out_*` intact server-side, and re-opening restores them through `.restore_selection()` (INT-01), so `selected_model()` is unchanged across the cycle. Pinned by two tests in `test-mod_1_06_model.R`: `expect_identical()` on the whole spec across collapse/re-open, and a forced exclusion still binding when the fit runs with the panel closed. One adjacent wrinkle recorded but deliberately not changed: the *first* open seeds force-include from `intersect(c(interactions, fixedeffects), choices)`, so the interaction variable joins the reported covariate list — numerically inert, since `build_formulas()` applies `unique()` to the RHS. · **UI-23 + UI-24** — export bundle (`R/fct_export.R`). Session-scoped artefact registry; registration goes through `session$userData`, which is shared between a root session and every module session beneath it, so no module's server signature or return API changed. Files follow one scheme, `NN_stepN_artefact-name.ext`, documented field-by-field in a generated `README.md` alongside a machine-readable `manifest.csv`. 14 table and 18 figure registrations across Steps 1-3; in every case the export and the on-screen output are driven by one shared builder (`build_stats_table()`, `build_weather_stats_table()`, `build_weather_binned_table()`, `make_regtable_df()`, and per-figure builders split out of their `renderPlot`), so a downloaded file cannot differ from what was displayed. Navbar `Export` menu: Export all (.zip) · Configuration only (.json) · Tables only · Figures only · Import configuration. The configuration snapshot is a single `reactiveValuesToList()` on the *root* input — module namespacing is a prefix on one flat map, so one pass captures every control and nothing is silently missed when a control is added — minus run counters and transient UI state, which would otherwise re-fire models on import; restore replays values with `sendInputMessage()` and retries for controls that only exist once their `renderUI()` has run. Archives use `utils::zip()` with a `zip`-package fallback and an actionable error when neither is available. Two writer defects were found on first real use and fixed: list columns (e.g. `selected_weather$polynomial`) abort `write.csv` with "unimplemented type 'list'", and the write sat *outside* the tryCatch, so one unwritable table propagated out of the download handler and Shiny answered with its HTML error page — which the browser saved under the `.zip` name. Non-atomic columns are now flattened, every write is guarded, and anything still unwritable is named in the README's "Not exported" section rather than silently missing. · **UI-25** — completion badges in the nav header (`step_badge_ui()` / `step_status()` / `render_step_badge()` in `utils_ui.R`, wired in `app_ui`/`app_server`): a check once a step's stored result matches its inputs, a reload arrow once an input has changed, nothing before the step has run. They read the per-step INT-08 stale flags rather than new state, so a badge and its in-page stale banner cannot disagree, and the Step 1 → 2 → 3 cascade follows for free. `nav_panel(value=)` is now set explicitly — it defaults to `title`, which would serialise the badge placeholder into the tab's `data-value`. `step_status()` rejects a non-reactive argument so a renamed upstream key fails loudly instead of leaving a permanently green badge. · **UI-26** — run provenance (`R/fct_provenance.R`). `wise_provenance()` builds an immutable record from each result's own `.snap` / `.sig` (never live inputs): source identity, model specification, random seed, an 8-character run signature, observation count, and any REACT-14 specification fallback. Credentials are stripped in exactly one place (`.provenance_source()`), so the record names *which* source the data came from without ever carrying the means to read it. The record is written into the export bundle's metadata, which is what makes an exported CSV traceable to a specific run. **Per user decision** there is no on-screen provenance banner: the Step 1 banner and its stylesheet were removed the same day, since the same information travels with the export and a permanent panel above every result surface cost more space than it earned. The REACT-14 fallback banner is unrelated and stays — it discloses that the fitted specification differs from the requested one. · **UI-32** — pre-run social-protection feedback in the Step 3 sidebar (`.sp_scenario_reach()`), showing eligible units (weighted, with the sample count beside it), share of population, transfer per unit and annual cost. The figures are the run's own numbers rather than an approximation: the preview calls `apply_policy_to_svy()` under the run's derived seed — so the inclusion/exclusion error draws are the ones the simulation will make — and totals the result with `.sp_transfer_totals()`, the single implementation of the arithmetic the diagnostics tab reports. It estimates over `hist_sim()$svy %||% survey_weather()`, matching `mod_3_06`; a first version used the full multi-round `survey_weather()` and overstated the population-level total by ~1.9x on a two-round survey while leaving the per-unit figure correct. All dynamically computed figures now render at one decimal through `fmt_num()` / `fmt_count()`, including the diagnostics table (previously `signif(x, 4)`, which showed `0.1234` beside `12340` in one column).

**Also fixed 2026-09-05** (defects surfaced while implementing the above, no prior register entry): Step 1's `Run model` silently no-opped until the "Model settings" flyout had been opened once — the flyout's `uiOutput` is hidden, hidden outputs are suspended, so `input$covariates` did not exist and `selected_model()` aborted on `req()`; the panel now renders eagerly (`suspendWhenHidden = FALSE`, matching `mod_1_04`) and the spec falls back to the defaults the sidebar displays, so a model with weather, the default interaction and default fixed effects but no covariates is runnable. · Selecting "Lasso" as the covariate method started the fit immediately from the sidebar: `lasso_result` was an `eventReactive` that `selected_model()` pulled on, and mod_1_07's stale-tracking observers forced it on selection; it is now a `reactiveVal` written only by the run-button observer (`priority = 100`, so it completes before the fit observer reads the spec). · Step 2 appended a fresh output tab on every run — the `results_tab_added` flag was set but only read on the removal path — and because each new tab carried another `#results_section`, `insertUI()` matched the *first*, filling the original tab and leaving the new ones empty; both Step 2 modules now guard the append and refresh the pane in place. · Step 3's staleness observers never fired after the first change: `.mark_stale_on_change()` was passed a reactive's *value*, and `observeEvent()` quotes the parameter symbol, so the promise was forced once and every later evaluation returned the cached value without re-registering the dependency; the helper now takes the reactive itself. · Step 3 ignored the first `Run simulation` click and worked on every one after: the trigger `req()`s the dynamically rendered button, so every pre-click evaluation threw and `ignoreInit = TRUE` spent its budget on the first *successful* one — i.e. the user's first click. `req()` already blocks the NULL and 0 states (an action button's 0 is not truthy), so `ignoreInit` was removed. An AST sweep confirmed no other `observeEvent` in the codebase pairs `ignoreInit` with a throwing event expression. · The Step 3 policy run aborted with no progress bar, notification or banner when an upstream reactive `req()`ed out, because the silent error propagated out of the observer; prerequisites are now read defensively and each missing one names itself, with a matching prerequisite banner and disabled run button in the sidebar (mirroring UI-29 in Steps 1/2). · An SP-only scenario is now explicitly a valid run (`.scenario_has_effect()` counts a non-zero transfer, which `.compute_policy_deltas()` deliberately excludes) and a literal no-op scenario warns rather than blocking. · The Step 3 SP accordion starts collapsed, so its suspended outputs meant an untouched panel reported `%||%` fallbacks including the retired `sp_type = "shock"`; those panels now render eagerly. · Every table gained a CSV control and the existing Step 2/3 buttons were restyled to a small, quiet "Download CSV" exporting all rows rather than the visible page; the weather summary tables were realigned to the presentation the other Step 1 summary tables use, and the `dt-wrap` rule they depend on was moved into `custom.css` — it had been a dangling `tags$style()` in `mod_1_02_surveystats_ui()` that was evaluated and discarded, so wrapped cells had never actually wrapped.

**Tag note:** in-code comments for this batch use working tags (`UI-42`..`UI-50`, `REACT-16`..`REACT-19`) that predate this reconciliation. They map to the register as: UI-45/UI-48 → UI-23 + UI-24, UI-47 → UI-25, UI-49 → UI-26, UI-32 → UI-32 (already aligned). UI-42/43/44/46/50 and REACT-16..19 correspond to the "Also fixed" defects above and have no register row of their own.

### 7.1 Recommended Output Delivery Sequence

1. **Exports first:** CSV of the tidy aggregated frame behind each result plot + server-rendered PNG from the same ggplot object. Include source/model/seed/run signature in filenames or a sidecar; raw household-row export requires a disclosure policy and is out of scope for the default UI.
2. **Selective interactivity second:** only aggregated decision charts; preserve the static ggplot path for publication PNG and as fallback.
3. **No global conversion:** household-level distributions, residual diagnostics, binscatters, and policy histograms remain server-rendered; Leaflet maps are already interactive.

---

## 8. Redundancy & Dead Code (`RED-*` & `DUP-*`)

- **RED-01** `inst/mathjax/` no longer exists; the prior ~100 MB deletion recommendation is stale. No action.
- **RED-06** Six near-identical simulation scripts (`batch/04_run_sim_*.R`, ~11,500 lines) → single canonical script parameterized via `Sys.getenv("WISEAPP_COUNTRY")` + YAML config.
- **RED-09** ⚠️ **PARTIALLY DONE**: `.Rbuildignore` anchoring fixes applied and the file is now tracked in git (commit `5550efc`). README re-render still open (`covr` unavailable locally).
- **DUP-03** `fct_policy_sim_compare.R` mirrors plotting/aggregation internals of `fct_sim_compare.R` → extract shared series assembly, threshold tables, exceedance rendering.

**Done (2026-08-31), compressed:** RED-02 (golem utils trio deleted, NAMESPACE regenerated) · RED-03 (`dev/archived_fct/` deleted) · RED-04 (byte-identical `dev/` vs `batch/R/` scripts deduped) · RED-07 (`fct_h3_check.R` deleted) · DUP-02 (weaker `.normalise_ssp()` duplicate deleted).

**Done (2026-09-01), compressed:** DUP-01 (shadowed `resolve_band_q` duplicate deleted; `fct_sim_compare.R` authoritative, key-contract tests added) · RED-05 (never-executed `spec_curve.R` moved to `dev/archive/`).

**Done (2026-09-02), compressed:** RED-08 — the seven one-off `dev/test_*.R` scripts plus `bench_lasso.R` and `scope_survey_weather_merge.R` moved to `dev/archive/` (`git mv`); the two `dev/bench_lasso.R` comment references in `fct_fit_model.R` updated to the archived path. `dev/` now holds only the golem lifecycle scripts, `run_dev.R`, `config_attachment.yaml`, and the `mod1_test/`/`outputs/` working dirs.

---

## 9. Testing & Packaging (`TEST-*`)

| ID | Issue & Location | Actionable Fix |
|---|---|---|
| **TEST-03** | ⚠️ **PARTIALLY DONE**: `Suggests` complete (incl. `covr`, 2026-09-01); edition 3 set; renv.lock already had `testthat`/`spelling`/`covr`. | `covr` used in `README.Rmd` still unavailable locally for re-render. |
| **TEST-05** | ⚠️ **PARTIALLY DONE**: edition-3 suite fully passes; `R CMD build` succeeds; built-source `R CMD check` clean on every code-level section. | Remaining findings are environment-only on the check machine: PDF-manual ERROR/WARNING (no `pdflatex`), tidy HTML validator, clock skew. Re-verify on a machine with TeX. |

**Done, compressed:** TEST-02/TEST-04 (unused imports removed; CLAUDE.md inventory updated) · TEST-01 (direct failure-ledger tests for every pipeline stage, completing the direct-coverage list) · TEST-06 (prosperity_gap/avg_poverty delta-method gradients fixed; finite-difference validation for every smooth method) · TEST-07 (statistical contract: **raw survey weights**, asserted) · TEST-08 (runtime deps declared, Rd fixes, ASCII-fication, `R/globals.R`, `.Rbuildignore`) · TEST-09 (delta gradient corrected below \$28; point estimates unaffected, coefficient bands were).

---

## 10. Deployment, Connections & Security (`DEP-*` / `SEC-*`)

| ID | Issue & Location | Severity / Impact | Actionable Fix |
|---|---|---|---|
| **DEP-01** | ✅ Bundled `inst/duckdb_extensions/` binaries + fail-fast `.duck_load_ext()` (missing bundle → loud error; extension recorded only after a successful `LOAD`) (2026-09-01) | **Critical** | Remaining: verify a fresh-clone Connect deployment end to end. |
| **DEP-02** | ✅ Dead GCS-keyfile/HF-token controls removed (GCS exposes HMAC keys; HF documented public-repos-only); Azure help corrected to Account Key; blank S3/GCS inputs fall through to env credentials (2026-09-01) | **High** | Remaining: integration-test each supported auth contract against real backends. |
| **SEC-03** | Process-wide DuckDB connection, views, tokens, secrets have no session-end cleanup (`R/fct_load_data.R:21-43, 128-179`) | **Low on one-session Connect; Medium for repeated local `run_app()` sessions** | Disconnect and clear caches in `session$onSessionEnded`; store token hashes rather than plaintext; prefer temporary views. |

**Done, compressed:** DEP-03 (local connects require `dir.exists()`; badge distinguishes configured from metadata-verified) · SEC-01 (`.sql_literal()` on every dynamic credential, adversarial quote tests) · SEC-02 (weather temp tables registered in a cleanup ledger with `on.exit` drop on all paths) · SEC-04/SEC-05 (data-derived popups and DT labels escaped).

### 10.1 Git-backed Posit Connect deployment (config as of 2026-09-02; commits `0b73a4c`, `459d3f7`, `fdd3960`; updated 2026-09-03 for the MapLibre/H3 engine)

- **2026-09-03 note (MapLibre/H3 cell migration):** the browser engine now ships as **vendored static assets** under `inst/app/www/vendor/` (`maplibre-gl.js`/`.css` 5.24.0, `h3-js.umd.js` 4.1.0 — sha256 pins in `R/fct_hexmap.R`) plus `inst/app/www/hexmap.js`, attached through one `htmlDependency` in `app_ui.R`. No R dependency changes, no renv/manifest churn: `DESCRIPTION` and `renv.lock` are untouched, the manifest regen procedure is unchanged, and the `sf`/`mapgl` `jq` checks still apply (`jq -r '.packages | has("sf")' manifest.json` → `false`, `has("mapgl")` → `false`). The only runtime network fetch beyond the app's own assets is the keyless CARTO vector Positron style URL baked into `hexmap.js` (same provider/risk profile as the raster tiles it replaces; if CARTO retires it, the documented fallback is switching `CARTO_POSITRON_STYLE` to the raster style or `Esri.WorldGrayCanvas` in the Leaflet fallback). A `bundle_resources()` alphabetical re-scan also serves the vendored scripts; `hexmap.js` is load-order tolerant and idempotent, and the explicit dependency always loads last, so ordering is guaranteed regardless.
- Connect pulls straight from this GitHub repo (poll or "Update Now") instead of a push-button `rsconnect::deployApp()` bundle. This requires a committed root `manifest.json`; `app.R` runs via `pkgload::load_all()` because `wiseapp` is never installed into the Connect library.
- **Regenerating the manifest:** whenever dependencies or the shipped file set change, run in `dev/03_deploy.R`: `rsconnect::writeManifest(appDir = ".", appFiles = c("app.R", "R", "inst", "man", "DESCRIPTION", "NAMESPACE"), appPrimaryDoc = "app.R")`, then **re-strip `sf`** from `manifest.json$packages` before committing — the Connect host cannot build `sf` (2026-09-02 incident: GDAL 3.10.2 headers and `gdal-config` exist under `/usr/local`, but the runtime `libgdal.so.36` is absent, so `configure` fails on the PROJ runtime check with error code `r-missing-system-library`; fix requires Connect admins to install the GDAL/GEOS/PROJ runtime libraries and `ldconfig`). The strip is safe because nothing in the executed app loads sf: leaflet lists `sf (>= 0.9-6)` in Imports, but its NAMESPACE only registers S3 methods for sf classes (no `import(sf)`), and `batch/` (the only sf user) is excluded from `appFiles`. ⚠️ **History note:** the 2026-09-02 MapLibre migration (`mapgl` 0.5.0, keyless CARTO *vector* Positron) broke this rule fatally — mapgl hard-Imports `sf`/`terra`/`geojsonsf` in both DESCRIPTION and NAMESPACE (`import(sf)`/`import(terra)`/`import(geojsonsf)`), so the sf strip failed `R CMD INSTALL` of mapgl, and pointing the three at PPM centos8 binaries then failed at `configure` on the broken host GDAL. The maps were rolled back to **leaflet** (same day): per-feature colours survive via leaflet's native `feature.properties.style` merge in `addGeoJSON` (one layer per map, same as mapgl's single fill layer), the CARTO raster Positron basemap is back (keyless; if CARTO retires unauthenticated raster tiles, the fallback is `providers$Esri.WorldGrayCanvas`), and `geojsonsf`/`classInt` drop out of the dep scan automatically once mapgl leaves Imports. `terra` **stays** in the manifest: `raster` (a leaflet dependency) requires it and terra builds without GDAL. Verify with `jq -r '.packages | has("sf")' manifest.json` → `false` and `jq -r '.packages | has("mapgl")' manifest.json` → `false`, then commit the diff.
- **`brand.yml` must stay in `Imports`:** `app_ui.R` passes `bs_theme(brand = app_sys("app/_brand.yml"))` on every launch, and `bslib:::brand_resolve.character()` hard-checks the `brand.yml` package via `rlang::check_installed()` — a missing package aborts the app on Connect. The TEST-08 dependency audit (2026-09-01) demoted it to `Suggests`, which the static scan cannot see and would silently drop from a regenerated manifest; it was moved back to `Imports` on 2026-09-02 before regenerating the manifest for `collapse`.
- **DuckDB extensions:** `httpfs`, `h3`, `spatial` are committed under `inst/duckdb_extensions/` as gzipped **v1.5.5 binaries matching the Connect library's DuckDB version** (version drift breaks `INSTALL`); `.duck_load_ext()` prefers the `.gz` (DuckDB decompresses on INSTALL) and falls back to uncompressed. `renv.lock` is pinned to the matching DuckDB.
- Renew this manifest/binary pairing on every DuckDB or dependency upgrade; a stale binary version breaks extension `INSTALL` on Connect with a loud error (fail-fast since 2026-09-01), not silent breakage.
